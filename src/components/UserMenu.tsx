"use client";

/**
 * UserMenu
 * --------
 * Header fallback component.
 * For USER UI → sidebar is used instead (no hamburger).
 * For SELLER / PUBLIC → safe to render nothing.
 *
 * DO NOT DELETE (used by Header).
 */

export default function UserMenu() {
  return null;
}
